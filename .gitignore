refs/
bin/
obj/
obj_dir/
config.h
*.log
*.fst
*.vcd
run/
test/**/*.out
test/**/build/
# Agent worktrees and per-agent state. `git worktree add .claude/worktrees/...`
# puts another agent's full checkout in here; committing it would nest a second
# copy of the repo inside itself.
.claude/
